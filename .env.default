# ─────────────────────────────────────────────────────────────────────────────
# .env.default — Fastlane environment template
#
# Safe to commit — contains placeholder values only.
# Override locally by creating an .env file (git-ignored) in this directory,
# or by injecting real values as CI secrets (GitHub Actions → Settings → Secrets).
#
# Fastlane loads this file automatically for all lanes.
# Secrets required only for `beta`, `release`, `certs` lanes (not `tests`).
# ─────────────────────────────────────────────────────────────────────────────

# ── App / Team identifiers ────────────────────────────────────────────────────
# Your Apple ID (developer account email).
APPLE_ID=you@example.com

# 10-character Apple Developer Team ID  (Xcode → Signing → Team).
TEAM_ID=XXXXXXXXXX

# App Store Connect team ID  (numeric, found in App Store Connect URLs).
ITC_TEAM_ID=XXXXXXXXXX

# Bundle identifier — must match Xcode project and App Store Connect record.
APP_IDENTIFIER=com.securecloud.vault

# ── Match (code signing) ──────────────────────────────────────────────────────
# Private Git repo that stores encrypted certificates + provisioning profiles.
# Example: https://github.com/yourorg/securecloud-certs.git
MATCH_REPO_URL=https://github.com/yourorg/securecloud-certs.git

# Encryption passphrase for the Match repo.
# Set this in CI secrets — never commit the real value.
MATCH_PASSWORD=

# ── Apple App-Specific Password ───────────────────────────────────────────────
# Required by Fastlane for uploading to TestFlight / App Store.
# Generate at: https://appleid.apple.com → App-Specific Passwords
# Set this in CI secrets — never commit the real value.
FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD=

# ── App Store Connect API Key (alternative to App-Specific Password) ──────────
# Preferred for CI because it doesn't require 2FA.
# Create at: App Store Connect → Users & Access → Integrations → Keys
# Set these in CI secrets — never commit real values.
APP_STORE_CONNECT_API_KEY_KEY_ID=
APP_STORE_CONNECT_API_KEY_ISSUER_ID=
# Path to the downloaded .p8 key file (or base64-encoded content for CI).
APP_STORE_CONNECT_API_KEY_KEY=

# ── Slack notifications (optional) ───────────────────────────────────────────
# Incoming webhook URL for build/release notifications.
# Leave empty to disable Slack notifications.
# Set this in CI secrets — never commit the real value.
SLACK_WEBHOOK_URL=

# ── CI / Xcode settings ───────────────────────────────────────────────────────
# Destination used by the `tests` lane.
FL_RUN_TESTS_DEVICE=iPhone 16 Pro

# Skip uploading symbols to Apple (useful if dSYMs aren't needed yet).
UPLOAD_SYMBOLS_TO_CRASHLYTICS=false

# ─────────────────────────────────────────────────────────────────────────────
# To configure a local environment:
#
#   cp .env.default .env
#   # edit .env with your real values (it is git-ignored)
#
# GitHub Actions secret names expected by the workflows:
#   APPLE_ID, TEAM_ID, ITC_TEAM_ID
#   MATCH_REPO_URL, MATCH_PASSWORD
#   FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD  (or API Key vars above)
#   SLACK_WEBHOOK_URL  (optional)
# ─────────────────────────────────────────────────────────────────────────────
